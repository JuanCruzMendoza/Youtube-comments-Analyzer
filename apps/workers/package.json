{
    "name": "workers",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "sst dev",
        "dev:local": "tsx src/local-worker.ts",
        "build": "tsc --noEmit && echo 'Skipping sst build (requires AWS creds)'",
        "deploy": "sst deploy",
        "remove": "sst remove",
        "console": "sst console",
        "type-check": "tsc --noEmit"
    },
    "dependencies": {
        "@google/generative-ai": "^0.24.1",
        "@repo/prompt-kit": "workspace:*",
        "@repo/shared": "workspace:*",
        "@supabase/supabase-js": "^2.86.0",
        "@upstash/redis": "^1.35.7",
        "googleapis": "^166.0.0",
        "sst": "^3.0.0"
    },
    "devDependencies": {
        "@repo/config": "workspace:*",
        "@types/node": "^20.11.24",
        "dotenv": "^17.2.3",
        "tsx": "^4.20.6",
        "typescript": "^5.3.3"
    }
}